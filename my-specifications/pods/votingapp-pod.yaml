apiVersion: v1
kind: Pod
metadata:
  name: votingapp-pod
  labels:
    name: votingapp-pod
    app: demo-voting-app
spec:
  containers:
    - name: votingapp
      image: dakshayahuja/votingapp-vote
      ports:
        - containerPort: 80